import 'package:flutter/material.dart';

class PastAttempts extends StatelessWidget {
  const PastAttempts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
